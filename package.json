{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "prettier --check .",
    "lint:fix": "prettier --write ."
  },
  "jest": {
    "testEnvironment": "jsdom"
  },
  "devDependencies": {
    "jest": "^30.0.4",
    "jest-environment-jsdom": "^30.0.4",
    "prettier": "^3.6.2"
  }
}
